server {
    listen 80;
    server_name artestramas3d.it www.artestramas3d.it;

    # Redirect HTTP to HTTPS (decommentare dopo aver configurato SSL)
    # return 301 https://$host$request_uri;

    # Frontend
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:8001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# HTTPS - Decommentare dopo aver configurato SSL con certbot
# server {
#     listen 443 ssl;
#     server_name artestramas3d.it www.artestramas3d.it;
#
#     ssl_certificate /etc/letsencrypt/live/artestramas3d.it/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/artestramas3d.it/privkey.pem;
#
#     location / {
#         proxy_pass http://frontend:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
#
#     location /api/ {
#         proxy_pass http://backend:8001/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
